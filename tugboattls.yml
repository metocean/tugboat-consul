daemon:
  build: ./docker/
  net: host
  volumes:
  - "#{DOCKER_CERT_PATH}:/certificates"
  - "examples:/consul"
  - "examples:/tugboat"
  environment:
  - DOCKER_HOST=
  - DOCKER_TLS_VERIFY=
  - DOCKER_CERT_PATH=/certificates
  - CONSUL_HOST=
  - "TUGBOAT_HOST=#{HOST}"